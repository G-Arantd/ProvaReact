import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
  buttonText: {
    color: '#ffffff',
    fontSize: 18
  },
  touchableContainer: {
    borderRadius: 5,
    justifyContent: 'center',
    alignItems: 'center',
    flex: 1,
  },
});